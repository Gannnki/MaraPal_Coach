# Cloned upstream knowledge base — reproduced by ingest/clone_wiki.sh, not committed.
# It is a git repo of its own; committing it would nest one repo inside another.
data/raw/running-wiki/

# Fetched data. Regenerable, dated, and large.
data/raw/races/
data/processed/
data/vector/
eval/results/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# credentials environment
.env

# Editors / OS
.vscode/
.idea/
.DS_Store

[project]
name = "marapal"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.0",
    "pyyaml>=6.0.3",
    "qdrant-client>=1.15",
    "sentence-transformers>=5.0",
]
